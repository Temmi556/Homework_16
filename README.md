# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4.(ДЗ) Средняя длина слова

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2.  Печать массива
3.  Вычисление суммы элементов массива
4.  Поиск в массиве значения равного a
5.  (ДЗ) Макс значение в заданном интервале


## 📋 Задания к лаб. 16



## 📁 Структура проекта
```
Homework_16/
├── functions.h
├── functions.c
├── main.c
└── README.md
```
### 🔧 Реализованные функции

1. **`float funct_1(float x)`**  
   **Назначение**: Вычисление функции \( y = x^2 - \cos^2(2\pi x) \)  
   **Параметры**:  
   &nbsp;&nbsp;`x` — аргумент  
   **Возвращает**: значение функции (`float`)

2. **`int task_1(void)`**  
   **Назначение**: Ввод массива `A[10]` с клавиатуры, преобразование в `B[i] = 2·A[i]`, вывод таблицы и расчёт среднего арифметического массива `B`  
   **Параметры**: отсутствуют  
   **Возвращает**: `0` (успех)

3. **`int task_2(void)`**  
   **Назначение**: Генерация массива `C[100]` по формуле `funct_1(x)` при `x ∈ [1.0, 3.0]`, расчёт суммы и среднего арифметического  
   **Параметры**: отсутствуют  
   **Возвращает**: `0` (успех)

4. **`void count_words_and_letters(void)`**  
   **Назначение**: Подсчёт количества слов и непробельных символов в строке, вычисление средней длины слова (с поддержкой кириллицы)  
   **Параметры**: отсутствуют  
   **Возвращает**: ничего (`void`)  
   **Особенности**: использует `wchar.h`, `getwchar()`, `setlocale()`

5. **`double f(double x)`**  
   **Назначение**: Вычисление функции \( f(x) = x^3 + 3x - 3 \)  
   **Параметры**:  
   &nbsp;&nbsp;`x` — аргумент  
   **Возвращает**: значение функции (`double`)

6. **`double* full_elements(double* ptr_array, int n)`**  
   **Назначение**: Заполнение массива значениями `f(x)` при `x = 2.0, 2.01, …`  
   **Параметры**:  
   &nbsp;&nbsp;`ptr_array` — указатель на выделенный массив  
   &nbsp;&nbsp;`n` — размер массива  
   **Возвращает**: указатель на заполненный массив

7. **`int put_elements(double* ptr_array, int n)`**  
   **Назначение**: Вывод элементов массива в формате `[i] = значение`  
   **Параметры**:  
   &nbsp;&nbsp;`ptr_array` — указатель на массив  
   &nbsp;&nbsp;`n` — размер массива  
   **Возвращает**: `0` (успех)

8. **`double* calc_elements(double* ptr_array, int n)`**  
   **Назначение**: Удвоение каждого элемента массива: `ptr_array[i] *= 2.0`  
   **Параметры**:  
   &nbsp;&nbsp;`ptr_array` — указатель на массив  
   &nbsp;&nbsp;`n` — размер массива  
   **Возвращает**: указатель на изменённый массив

9. **`double sum_elements(double* ptr_array, int begin, int end)`**  
   **Назначение**: Сумма элементов массива в диапазоне `[begin, end]` (включительно)  
   **Параметры**:  
   &nbsp;&nbsp;`ptr_array` — указатель на массив  
   &nbsp;&nbsp;`begin`, `end` — индексы начала и конца  
   **Возвращает**: сумма (`double`)

10. **`int find_element(double* ptr_array, int n, double element)`**  
    **Назначение**: Поиск элемента в массиве с точностью `±0.1`  
    **Параметры**:  
    &nbsp;&nbsp;`ptr_array` — указатель на массив  
    &nbsp;&nbsp;`n` — размер массива  
    &nbsp;&nbsp;`element` — искомое значение  
    **Возвращает**: индекс найденного элемента или `-1`

11. **`double max_begin_end(double* ptr_array, int begin, int end)`**  
    **Назначение**: Поиск максимального элемента в диапазоне `[begin, end]`  
    **Параметры**:  
    &nbsp;&nbsp;`ptr_array` — указатель на массив  
    &nbsp;&nbsp;`begin`, `end` — индексы начала и конца  
    **Возвращает**: максимальное значение (`double`)

12. **`void generate_and_merge_arrays(void)`**  
    **Назначение**:  
    - Генерация трёх массивов (`A`, `B`, `C`) случайных чисел в диапазоне `[10, 50]`  
    - Формирование массива `D` по принципу чередования:  
      `D = [A₀, B₀, C₀, A₁, B₁, C₁, …]`  
    **Параметры**: отсутствуют  
    **Возвращает**: ничего (`void`)  
    **Особенности**: использует `srand()`, `malloc()`, `free()`

